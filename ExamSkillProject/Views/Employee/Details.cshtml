@model ExamSkillProject.Models.ApplicationUser
@{
    ViewBag.Title = "Details";
}

<h1>Employee details</h1>
@*<div class="col-md-12 info-data">
    <dl class="col-md-4 center-block">
        <dt>Full name</dt>
        <dd>@Model.FirstName @Model.LastName</dd>
        <dt>Email</dt>
        <dd>@Model.Email</dd>
    </dl>

</div>*@
<div class="col-md-12 info-data">

    <div class="col-md-4 center-block">
        <h2>@Model.FirstName @Model.LastName</h2>
        <img src="@Url.Content($"~/UploadedFiles/{Model.Picture}")" alt="Alternate Text" />
        <dl>
            <dt>First name</dt>
            <dd>@Model.FirstName</dd>
            <dt>Last name</dt>
            <dd>@Model.LastName</dd>
            <dt>Email</dt>
            <dd>@Model.Email</dd>
            <dt>Start date</dt>
            <dd>@Model.StartDate.Day/@Model.StartDate.Month/@Model.StartDate.Year</dd>
        </dl>
    </div>

</div>

@Html.ActionLink("Back", "Index")
